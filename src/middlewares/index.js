const validation = require('./validation');
const validateId = require('./validateId');
const authenticate = require('./authenticate.js');
const upload = require('./upload');

module.exports = { validation,  validateId, authenticate, upload };
